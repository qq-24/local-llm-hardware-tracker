# 蓝图 · 算力-价格-智力追踪体系（版本流程设计）

> 状态：**草稿 v0.1**（2026-08-27）
> 性质：本项目最终形态的完整设计蓝图。包含【已实现】资产盘点、【已定锚点】、【流程设计】与【待实现】清单。
> 用途：用户审阅后确认，再据此执行建设。蓝图本身会随项目演进更新。

---

## 一、项目本质（一句话）

追踪一条曲线：**成本（价格）基本不变时，能买到的本地 AI 智力随时间能涨多少**——固定三条约束线和一个"当期目标模型"，每期记录「满足该模型的本地硬件方案 + 当时真实价格」，观察"智力换钱"的效率如何随时间变好。

核心输出：`README.md`（即主追踪表，当前视图）+ 每期版本快照（不可变证据链）。

---

## 二、固定锚点（永远不变，改动需显式声明）

| 锚点 | 值 | 说明 |
| --- | --- | --- |
| C1 上下文 | **≥ 100 万 tokens** | 本表唯一"装得下吗"门槛 |
| C2 出字速度 | **≥ 50 t/s** | 本表真正卡死的门槛（带宽墙） |
| C3 预填充 | **≥ 1500 t/s** | 最宽松的一条 |
| 量化口径 | **权重 Q4 + KV Q8** | 所有显存判定统一按此，不随官方发布格式（如 FP8）变 |
| 1M 实现 | **YaRN 4 倍扩展**（262K×4） | 扩展本身不构成否决理由 |
| 目标模型范围 | **open weights；进内存总参 ≤250B；激活 ≤30B** | 进内存总参以 HF safetensors 实测为准 |
| 目标选定 | **范围内 AA Intelligence Index 最高者**；用户可随时手动指定覆盖 | 脚本自动判定 |
| 币种 | 人民币；美元按当日汇率标注换算值 | git 历史可回看当日汇率 |
| 价格显示 | **表格价格列自动进位**（千/万：9000→9千，18000→1.8万等），提升易读性 | 脚本/生成时格式化 |

---

## 三、总体架构（三层）

```
┌────────────────── 视图层 ──────────────────┐
│  README.md（主追踪表） ← 主表（当前视图，每版本自动更新） │
├────────────────── 证据层 ──────────────────┤
│  版本快照/<日期>-<关键词>/                   │
│    ├ 01-模型档案.md    （模型 Agent 取证）    │
│    ├ 02-硬件档案.md    （硬件 Agent 取证）    │
│    ├ 03-对账.md        （主线程合并+矛盾清单）│
│    └ 04-方案规划.md    （可执行建议，脚本生成）│
├────────────────── 工具层 ──────────────────┤
│  tools/fetch_aa.py   （AA+HF 自动抓取判定）  │
│  tools/format_price.py（价格自动进位格式化）  │
│  存档-内部工作稿/     （一次性研究产物底座）    │
└────────────────────────────────────────────┘
```

**运行性质：自动化任务。** 用户在最开始给一次输入（初始要求：目标模型范围/约束/本期指令），之后 Agent 全自主执行到产出最终成果，中途不打断用户；尽量把可确定的环节交给脚本，减少 Agent 自由发挥。

---

## 四、每期版本流程（自动化任务：先模型 → 后硬件 → 终方案）

### 触发与输入
- 触发：用户"想起来就做"（新模型发布 / 价格大幅变化 / 新硬件上市 / 单纯想记录）。
- **用户只在开始时给一次输入**：目标模型（可选，缺省走自动判定）、约束（沿用固定锚点）、本次要什么（默认全流程出成果）。
- 之后 **Agent 自主跑完全部步骤到产出最终成果**，中途不等用户。

### 步骤（按序执行，脚本优先，Agent 兜底）

```
┌─ 0. 锁定本期目标模型（脚本 tools/fetch_aa.py 完成判定）
│      自动判定：AA 全表 → 过滤 open weights → 激活≤30B
│        → 进内存总参≤250B → AA 分降序 → Top1 = 当期目标
│      用户可手动指定覆盖（输入目标模型名后脚本跳过自动判定）
│
├─ 1. 【模型档案 Agent】——读 HF config.json / safetensors / blog / tech report / AA 详情页
│      输出：架构、Q4 权重体积、Q8 KV@1M、合计显存门槛、
│            AA 分数、原生上下文、是否过 C1（装 1M）
│      → 得到"这个模型需要什么水平的机器"
│
├─ 2. 【硬件档案 Agent】——以 1. 的显存门槛为输入，全网收集"值得买的机器"
│      核心目标：找到最低的钱里，性能最高、容量最大的方案（最高性价比）
│      手段：
│      a) 【真机口碑】先逛知乎 / Linux.do / Reddit / X / HF 社区等，
│         看大家实际在用、在推哪台机器能跑 1M 大模型（人群验证过的性价比机器）
│         → 知道"该考虑哪些机"
│      b) 【价格】从 a 锁定的候选机，去闲鱼 / 电商 / eBay 等查当期真实行情
│         → 各档价格 + URL + 抓取日期
│      c) 汇总候选：组成 / 显存 / 容量 / 带宽 / 算力 / 价格 / 性价比排序
│     ★ 不是按规格表硬搜，而是"人先验证值不值，再给价"；重性价比而非堆配置
│
├─ 3. 【对账】模型档案与硬件档案交叉验证（两路独立，可并行）
│     冲突项 → 强制进 03-对账.md 矛盾清单，不静默取信
│     唯一口径数字标"单来源"
│     价格必须带 URL + 抓取日期
│
├─ 4. 【方案规划】基于 2.3. 产出可行动建议
│      （最便宜能装 1M 的机器是 X，出字差一倍；性价比最高路线是 Y）
│      → 为最终"买不买、买哪个"做好准备
│
├─ 5. 【表格更新·脚本+Agent】填追踪表本期行
│      a) 价格列用 tools/format_price.py 自动进位（9000→9千，18000→1.8万）
│      b) 其余字段结构化写入；表格格式统一，易读性优先
│
└─ 6. 【定稿】git commit = 版本固化；向用户交付摘要
```

### 每次版本产出
```
版本快照/<日期>-<关键词>/
├── aa-target.<日期>.csv/md   （自动判定快照，含完整候选表）
├── 01-模型档案-<模型名>.md
├── 02-硬件档案-<模型名>.md
├── 03-对账-<模型名>.md
└── 04-方案规划-<模型名>.md
```

### 自动化程度分层（脚本优先，减少自由发挥）
| 环节 | 工具 | 是否脚本 |
| --- | --- | --- |
| AA 抓取过滤判定 | `tools/fetch_aa.py` | ✅ 全脚本 |
| 价格进位显示 | `tools/format_price.py` | ✅ 全脚本 |
| HF 参数量/体积 | HF API 拉取 | ✅ 可脚本 |
| 模型档案读取 | 子代理（有明确读取目标+字段模板） | 半自动 |
| 硬件价格收集 | 子代理：先全网搜口碑（知乎/Linux.do/Reddit/X/HF 社区）锁定候选机，再查价（闲鱼/电商/eBay）；反爬处 Agent 兜底 | 半自动 |
| 表格写入 | 子代理按既有格式+脚本格式化价格 | 半自动 |

### 铁律
1. "两个人撕一份档案"：模型档案与硬件档案**两条独立取证链**，主线程对账；两路互不可见，杜绝抄同源。
2. 单一来源数字必须标"单来源"。
3. 价格必带 URL + 抓取日期；入库前经 `format_price.py` 统一进位。
4. 冲突强制进矛盾清单，不静默取信。
5. MoE 内存按**总参数**算（非激活参数）。
6. 表格易读性优先：价格自动进位、列宽字段统一、避免纯长数字堆砌。

---

## 五、已实现（盘点截止 2026-08-27）

### 主资产
- **`README.md`（主追踪表）**：固定约束 + 完整方案总览 S1~S20（三约束分别判定/价格/算力/带宽/选择）+ 观察记录 + 判定公式。顶部标注"更新于 2026-08-27"。
- **`AGENT.md`**：项目规则（量化口径、YaRN、成本锚点、版本流程、性能推算、闲鱼实录等硬规则）。
- **git 仓库**：版本历史即文档历史（已在 GitHub 托管）。

### 已核实的一级来源（对话推理 → 官方实测）
| 模型 | 状态 |
| --- | --- |
| Qwen3.5-122B-A10B | config 定案：12 层全注意力+36 层线性注意力；Q4 权重 62.5GB + KV@1M 12GB ≈ 75GB；"265GB 口径"排除 |
| Qwen3.8-Flash-Next | HF 卡 + 技术报告：125B/6B(+51B n-gram off-accel)、每4层1层 QSA、1M 需静态 YaRN |
| GLM-5.3-Flash | HF config+safetensors：kv_lora_rank 512、45 层、Q4≈160GB+KV@1M 23GB≈183GB；官方仅发 FP8(306GiB)，按 Q4 口径判定 |
| 智谱 Coding Plan | 三档积分+抵扣系数+高峰规则全核实；pricing 页 JS 抓不到，单价未验证 |

### 研究底座（存档-内部工作稿/）
7 份事实文档 + 1 份对话全量提取稿 + 19 条口径矛盾清单 → 追踪表的原始数据底座。

### 已验证的技术能力
- AA `/leaderboards/models` 全表可抓；AA `/models/<slug>` 可得 Total/Active 参数 + open weights + HF 链接 ✅
- HF API 得 safetensors 参数体积 ✅
- PDF 技术报告 pypdf 提取管线 ✅
- 双 Agent 独立取证对账的**一次样板执行已跑通过**（首次版：硬件提取 + 模型事实）✅

---

## 六、待实现（下一步建设）

| # | 项 | 状态 |
| --- | --- | --- |
| 1 | `tools/fetch_aa.py`：抓 AA → 过滤 open weights + active≤30 → total≤250 → AA 最高 → Top1 | 待写 |
| 2 | `tools/format_price.py`：价格自动进位显示（千/万）+ 表格格式统一 | 待写 |
| 3 | 运行脚本验证当期 Top1 = Qwen3.8-Flash-Next | 待跑 |
| 4 | 首个正式样板版本：2026-08-27-首期（模型档案→硬件档案→对账→方案→表格更新）| 蓝图批准后执行 |
| 5 | 追踪表目标模型机制化（自动判定 + 本期=Qwen3.8-Flash-Next 记录）+ 价格列表格升级为进位格式 | 待做 |
| 6 | README / memory 写入版本流程铁律与目标筛选规则 | 待做 |

---

## 七、执行顺序（蓝图批准后按序做）

1. 写 `tools/fetch_aa.py`（抓取+过滤+判定，含人工覆盖参数）
2. 写 `tools/format_price.py`（价格进位格式化）
3. 跑脚本 → 落 aa-target 快照 → 验证 Top1=Qwen3.8-Flash-Next
4. 建 `版本快照/2026-08-27-首期/`，并行两路 Agent 取证（模型档案 + 硬件档案）
5. 主线程对账 → 03 对账.md → 04 方案规划.md
6. 更新追踪表（含价格进位列）+ README + memory
7. git commit

---

*本蓝图随项目演进持续维护。被取代的旧设计移入 存档-旧版已取代/。*
