# 算力卡盘点 · 本地模型运行

**一句话**：固定"最低 agent 可用智力"线，长期追踪满足它的本地部署方案，价格能便宜到什么程度；同时观察同一价格下，买到的本地模型智力随时间涨了多少。

## 目录结构与使用

| 文件 | 作用 | 更新频率 |
| --- | --- | --- |
| `README.md`（追踪表） | **主追踪表**：固定约束（1M 上下文 / ≥50 t/s 出字 / ≥1500 t/s prefill）→ 硬件方案档案 → 按日期的观察记录。所有新增观察都往这里追加 | 每月 / 想到就记 |
| `蓝图-版本流程设计.md` | 项目最终形态设计蓝图（已实现资产 + 流程 + 待办），本规则的出处 | 需求变化时 |
| `version快照/` | 每期版本的取证原件：`01-模型档案` `02-硬件档案` `03-对账` `04-方案规划` | 每期一次 |
| `tools/fetch_aa.py` | **AA 目标模型自动判定**：抓 AA 榜单 → open weights + 总参≤250B + 激活≤30B → 智力最高 = Top1 | 每期跑一次 |
| `tools/predict_perf.py` | **纯推算引擎**（无实测）：架构+硬件规格 → 公式链 → decode/prefill 预测（利用率 0.8 固定、多卡 TS 通信模型，全部标注[推算]） | 每期跑一次 |
| `tools/hw_specs.json` | 硬件规格表（带宽/算力/互联），双源交叉内附来源 | 硬件变更时更新 |
| `tools/xy_search.py` | **闲鱼现货采集**：复用 ai-goofish-monitor 项目导出的登录态（见脚本内说明），搜关键词→真实在售价→价格基线表 | 每期跑一次 |
| `tools/format_price.py` | 价格自动进位格式化（9000→9千 / 18000→1.8万） | 表格写入时 |
| `存档-内部工作稿/` | 一次性研究产物（事实文档/提取稿/矛盾清单/双源素材），追踪表的原始数据底座 | 数据源变化时 |

## 版本流程（自动化任务）

用户只在开始时给一次输入（目标模型可选/约束沿用/要什么），之后全自主跑完：

```
0. 锁定目标模型：fetch_aa.py 自动判定（或--model 手动指定覆盖）
1. 模型档案 Agent（01）：读 HF config/safetensors/blog/tech report/AA → 算 Q4权重+Q8 KV@1M+门槛
2. 硬件档案 Agent（02）：以模型门槛为输入，全网找"最低钱里性能最高容量最大"的机器
   （先知乎/Linux.do/Reddit/X/HF社区锁口碑 → 再闲鱼/电商/eBay查价 → 性价比排序）
3. 性能推算：predict_perf.py 纯计算预测 decode/prefill（无实测，[推算]）
4. 价格实录：xy_search.py 抓闲鱼现货真实在售价 → 价格基线
5. URL 验证 Agent：逐个打开硬件档案来源 URL 核对内容（防幻觉）
6. 对账（03）：两条独立取证链交叉 + 推算/实测对照，冲突进矛盾清单
7. 方案规划（04）：买不买/买哪个/怎么验收
8. 更新追踪表：价格经 format_price.py 进位，C2/C3 填推算值
9. git commit = 版本固化
```

## 性能推算规则（predict_perf.py）

- **纯计算，不依赖实测**：decode 按带宽墙、prefill 按算力墙，公式全透明可复核
- 利用率固定 **0.8**；MoE 路由开销 1.3；KV 读占比 0.25；多卡 TS 通信 k=0.3
- 多卡效率按卡间互联：PCIe x4 灾难性 / x16 一般 / NVLink 优
- 所有结果显示 `[推算]` 标注，绝不冒充实测；参数可调

## 记录流程（每次"想到"都这么干）

1. 打开 `README.md`（即追踪表），在"观察记录"下加一条**当日新记录**（日期 + 新增信息）。
2. 数字来源分级标注：**实测引用 / AI推算 / AI搜索引用 / 用户提供**，没有一手来源的不写死。
3. 价格变化、新方案出现、新模型压小 KV、实测数据，都值得记。
4. 完成后如有 git，提交一条中文 commit。

## 硬规则（不要破坏）

- **量化口径 = 项目标准**：本地运行统一用 **Q4 量化模型权重 + Q8 量化 KV Cache**。所有显存/内存判定与硬件方案评估一律按此口径，不随模型官方发布格式（如 FP8）改动。
- **1M 上下文统一按 YaRN 4 倍扩展口径判定**：原生 262K 模型 × 4 = 1M，扩展本身不构成 C1 的否决理由。
- C1/C2/C3 三条约束是**锚点，永远不改**；要改只能调目标模型（同体量换代），改模型时必须显式声明，便于回看"这条线当时的含义"。
- **版本铁律（"两个人撕一份档案"）**：每期最终档案必须由**两条独立取证链（模型 Agent + 硬件 Agent）各自取证、主线程对账**产出；单一来源数字必须标"单来源"；价格必带 URL+抓取日期；冲突强制进矛盾清单；MoE 内存按**总参数**算。
- **性能 = 纯推算，不依赖实测**：速度一律用 `predict_perf.py` 公式链计算并标 `[推算]`；没有"等实测"这种空头支票，也不让用户去租卡实测。
- **价格 = 闲鱼/电商现货实录优先**：能用 `xy_search.py` 抓真实在售价就用实录价；纸面价/检索价标 [F] 且低于实录价的视为过时。
- **目标模型自动判定**：open weights + 进内存总参 ≤250B + 激活 ≤30B → AA Intelligence Index 最高者；用户可手动覆盖。
- MoE 内存按**总参数**算（不是激活参数），否则会严重低估。
- 价格一律人民币，美元标注当日汇率换算值；**表格价格列自动进位**（千/万）提升可读性。

## 待办（下次继续的开头）

1. 给 4080S/2080Ti 魔改补硬件规格进 hw_specs.json（当前引擎缺这两档）。
2. 空闲时用 xy_search.py 补采更多词（4090魔改/2080Ti/Strix Halo）。
3. 170HX 掉价 / 新卡上市，记一条新观察。
4. CMP 170HX 带宽 900/1500 验证（影响推算结果）。